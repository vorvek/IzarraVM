# Intermediate build artifacts from build-freedos.ps1. The committed sources of
# truth are the FreeDOS-built binaries under crates/izarravm-firmware/roms
# (tokados-hdd.img, gswmode.com, tokaemm.sys) plus the vendored FreeDOS trees'
# own .gitignore rules under freedos/.
/build-freedos-tokamous.com
