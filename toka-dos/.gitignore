# Intermediate build artifacts. The committed source of truth is the packed
# tokados.rom under crates/izarravm-firmware/roms, produced by build.ps1.
/build/
# The authoring-only packer's Rust build output.
/pack/target/
