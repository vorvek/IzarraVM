# This file is part of IzarraVM and is licensed under GNU GPL version 3 only.
# SPDX-License-Identifier: GPL-3.0-only

[package]
name = "izarravm-bus"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[features]
# S1d slice 2 of the shadow-cache-differentiation design's shadow-cache probe:
# the entry-sampling hook on CpuBus (`shadow_probe_should_sample_entry`). NOT in
# any default feature set -- forwarded from izarravm-machine/izarravm like
# reflected-call-diagnostic; see izarravm-machine's Cargo.toml.
shadow-cache-probe = []

[dependencies]
thiserror.workspace = true
