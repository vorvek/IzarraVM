; probes/probe-pit.inc - STREAM C slot. Foundation stub: empty.
; Stream C adds POST_STEP "component.timer_pit", probe_pit and the handler
; (counter-latch readback on PIT channel 0, independent of PIC state).
