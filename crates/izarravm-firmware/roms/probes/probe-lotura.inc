; probes/probe-lotura.inc - STREAM C slot. Foundation stub: empty.
; Stream C adds POST_STEP "component.cpu_lotura", probe_lotura (PASS/FAIL) and
; POST_STEP "cpu.gsw_mode", probe_lotura_mode (MEASURE) plus the handlers, which
; read PORT_LOTURA_ID (== LOTURA_ID_VALUE) and PORT_LOTURA_MODE.
