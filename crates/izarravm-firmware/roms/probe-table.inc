; probe-table.inc - STREAM C shared slot (reserved). Each peripheral probe
; registers itself with POST_STEP inside its own probes/probe-*.inc file, so the
; step table needs no pre-sizing here. Reserved for cross-probe constants if a
; later Stream C task needs shared data. Foundation stub: empty.
